Flask
gunicorn
scikit-learn
numpy
pandas
joblib
requests
python-dotenv
# --- Start of PyTorch/Sentence-Transformers optimization ---
# IMPORTANT: The version of torch below (2.3.0+cpu) is an example.
# You MUST find the correct CPU-only version for Render's Python environment.
# Go to pytorch.org/get-started/locally/, select 'Linux', 'Pip', 'Python', 'CPU'.
# Note down the exact version (e.g., torch==2.5.1) and use it here.
# For example, if Render is using Python 3.12, the PyTorch site will give you a specific version.
torch==2.3.0+cpu # Replace 2.3.0 with the actual version you find on PyTorch site for CPU.
sentence-transformers
# --- End of PyTorch/Sentence-Transformers optimization ---
PyMuPDF
python-docx